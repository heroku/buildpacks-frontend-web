{
  "name": "@heroku/env-as-html-data-wasm",
  "version": "0.0.0",
  "description": "Inject PUBLIC_WEB environment variables into HTML head data attributes using WebAssembly.",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "bin",
    "index.d.ts",
    "index.js",
    "lib",
    "pkg"
  ],
  "bin": {
    "env-as-html-data": "bin/env-as-html-data"
  },
  "scripts": {
    "build": "rm -rf pkg && wasm-pack build ../../common/env_as_html_data_wasm --target nodejs --release --out-dir ../../support/env-as-html-data-wasm/pkg && rm pkg/.gitignore",
    "pretest": "npm run build",
    "test": "node --test"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "glob": "^13.0.6",
    "toml": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
